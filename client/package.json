{
  "name": "phaser-mmo-client",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist",
    "deploy": "npm run build && npm run start"
  },
  "dependencies": {
    "colyseus.js": "^0.16.16",
    "phaser": "^3.88.2",
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
